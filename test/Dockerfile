FROM tomashgombosh/chrome-firefox:latest

RUN java --version && sleep 5 && google-chrome --version && sleep 5 && firefox --version && sleep 5 && chromedriver --version && sleep 5 && geckodriver --version

